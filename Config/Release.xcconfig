// Release Configuration
// Includes secrets via the per-config Release layer (§6.1).

#include "Secrets-Release.xcconfig"

// Release-specific settings can go here
// Example: SWIFT_OPTIMIZATION_LEVEL = -O
