// Debug Configuration
// Includes secrets via the per-config Debug layer (§6.1).

#include "Secrets-Debug.xcconfig"

// Debug-specific settings can go here
// Example: SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG
