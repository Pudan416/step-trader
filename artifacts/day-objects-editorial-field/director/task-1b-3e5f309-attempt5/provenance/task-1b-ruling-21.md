# Task 1B Visual Director Ruling 21 — Verbatim Blind-Lock Preservation

RULING: The Gate 8 staged set is not commit-ready because `git diff --cached --check` correctly rejects three trailing-space lines in Critic A's exact blind-lock Markdown. Do not normalize or alter the authoritative lock bytes. In the newly created target director root only, replace the staged Markdown copy with a deterministic base64 verbatim payload, a decoded-byte SHA-256 record, and a readable non-authoritative Markdown transcription that uses explicit `<br>` tags instead of trailing spaces. Preserve the original central SDD lock unchanged. Critic B's lock and both exact four-line verdicts remain byte-for-byte copies. Re-run the full staged allowlist and diff checks; no material package, approval fact, verdict, source, or production byte may change.

EVIDENCE: The freeze implementer staged exactly 1252 allowlisted paths totaling 190788157 bytes, with zero forbidden baseline, temporary, prior-attempt, Metal, canvas, or golden paths and no file larger than 1362256 bytes. The sole staged-check failure is three `trailing whitespace` findings at `critics/critic-a-blind-lock.md` lines 3–5, where the source intentionally uses Markdown hard-break spaces. Removing those spaces would change the authoritative lock SHA-256 `a3e2cc6095ceaafc0cb91d3327d688d761135cad0d897d66319936fb32f90b4b`; accepting a dirty diff would violate the Gate 8 check. Lossless base64 preserves every original byte while making the Git payload itself whitespace-clean and independently decodable.

LARGEST GAP: The freeze cannot pass its mandatory staged diff check while the exact Critic A lock is stored as ordinary Markdown containing intentional end-of-line spaces.

NEXT ACCEPTANCE TEST: Decode the staged verbatim base64 payload and require exact SHA-256 `a3e2cc6095ceaafc0cb91d3327d688d761135cad0d897d66319936fb32f90b4b` plus byte equality with the untouched central source; confirm the readable transcription is explicitly non-authoritative. Then require `git diff --cached --check` exit0, the same strict allowlist/forbidden-path/size checks, exact verdict hashes, all package source-copy comparisons and seals, and a complete freeze-preparation report before independent review.
