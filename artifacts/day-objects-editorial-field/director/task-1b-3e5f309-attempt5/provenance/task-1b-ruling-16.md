# Task 1B Visual Director Ruling 16 — Attempt 5 Gate 1–6 Authorization

RULING: Attempt 5 passes Gate 0 at exact source `3e5f3095167889b3a9ae5a8145ef0103489e6e3c`. The isolated producer is authorized to create the new fixed `approved-3e5f309-attempt5` scale-1 candidate/rerender and scale-3 candidate/rerender packages sequentially, then execute Gates 1–6 exactly as defined by the Task 1B protocol. Each package becomes immutable at creation. Exactly one heavy render or verifier may run at a time. Aesthetic critics and material approval remain forbidden until semantic completeness, tamper rejection, untouched-original reverification, byte-identical rerenders, cross-scale native-frame equality, blind A/B construction, and a director pre-critic audit all pass.

EVIDENCE: The sole Attempt 5 full suite completed with exit `0`, exact `111/111` tests and `5/5` suites, zero failure/issue markers, framework duration `5076.237s`, wrapper duration `5118s`, maximum RSS `4068261888` bytes, and process swaps `0`. The full-suite log SHA-256 is `814cd317a1e6c1e9123cdd5021c13b9b0ab3b2742972cfabc8bb60dc94319516`; the exit-sidecar SHA-256 is `d308957c02525d520422f99172fb29412e8c57f42c2d024ac88dbf67df52bd33`; the producer report SHA-256 is `9b741d181e97d14d26c14dcade50539884eb204203ba4f044e26e534786eb469`. All 510 monitor samples preserved cumulative `Swapouts = 915659` and `Pages throttled = 0`, so Swapouts delta is `0`; pre-existing allocated swap fell from `4111.50M` to `3503.50M`. Postflight reconfirmed a clean exact checkout, unchanged visible manifest, composition approval, composition recipes, and renderer SHA-256 `e2170a3f1f3a9798bd857f059fdee9b53b0fb2e1d4f16754f244a62236a2d543`, no remaining heavy process, absent material root, and no prior render.

LARGEST GAP: No immutable Attempt 5 material package, deterministic rerender proof, semantic inventory, tamper proof, or blind A/B packet exists yet, so there are no bytes eligible for aesthetic judgment.

NEXT ACCEPTANCE TEST: From the same clean detached checkout, generate each of the four fixed package directories exactly once with its declared scale and source commit, serializing every heavy command. Verify each untouched package, reject a one-byte mutation only in a disposable copy, reverify the original, and prove candidate/rerender recursive byte identity at both scales. Then prove the exact 18 native outline frames, actor/topology crops, canonical fixtures, catalog sheets, 162-frame family atlas, 90 authority records, trace replay, cross-scale native equality, and sealed hashes; create the deterministic same-seed blind A/B packet and pass a director-only pre-critic leak/inventory/hash audit. Any mismatch stops without repair or overwrite; only complete Gates 1–6 may commission exactly two fresh isolated critics.
