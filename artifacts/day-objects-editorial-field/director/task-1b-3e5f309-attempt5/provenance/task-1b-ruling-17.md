# Task 1B Visual Director Ruling 17 — Semantic Auditor Enumeration Fix

RULING: The first Gates 2–5 audit is rejected as a producer-auditor harness failure, not a package failure. Preserve its command, exit, output, resources, and hashes unchanged. Authorize one corrected, distinctly labelled read-only audit that removes dependence on Foundation's `.skipsHiddenFiles`, first proves enumeration against a disposable directory carrying the macOS `hidden` flag, and then audits the four sealed packages without writing to them. No rerender, verify-material retry, package repair, overwrite, or reseal is authorized.

EVIDENCE: The failed audit exited `1` in ten seconds with only `AUDIT: FAIL / scale1 expected 261 files`, output SHA-256 `16990eea95b204f75091e9932929b0be47a5a51f93a1dcdd0c659028046e40d4`, zero process swaps, zero Swapouts delta, and zero throttled pages. Independent director inspection with `find` counted exactly `261` regular files and zero missing roots in each of `scale1-candidate`, `scale1-rerender`, `scale3-candidate`, and `scale3-rerender`; `ls -ldeO` showed the `hidden` flag on every package directory. The audit's Foundation enumerator used `.skipsHiddenFiles` and returned zero descendants under those roots. All four prior seals remain unchanged, no package write occurred after launch, and Gate 6 has not started.

LARGEST GAP: Gates 2–5 still have no valid semantic verdict because the only audit invocation discarded all files before evaluating the required frames, crops, contact sheets, authority records, and cross-scale equality.

NEXT ACCEPTANCE TEST: In director-only disposable space, create a hidden-flag fixture containing visible files and prove the corrected enumerator returns the exact inventory while excluding only explicitly disallowed dot entries by name. Then run one new labelled audit over the existing sealed packages, require exact 261 files per package, all Gate 2–5 counts and paths, native dimensions/crop equality, 90 canonical authority records with replay-consistent metadata, candidate/rerender identity, and cross-scale native-frame equality. Recompute the four pre/post recursive seals and require byte identity. A PASS may proceed to Gate 6; any failure stops without another retry.
