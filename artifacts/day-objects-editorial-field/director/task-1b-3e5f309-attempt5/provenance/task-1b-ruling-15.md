# Task 1B Visual Director Ruling 15 — Attempt 4 Resource Adjudication

RULING: Attempt 4 remains immutable and is rejected at Gate 0 because its sole full-suite invocation was interrupted before a complete result. The material root stays absent and no render is admitted. Ruling 14's phrase `zero swaps` is clarified prospectively: a host's pre-existing allocated swap is historical state and cannot be attributed to a new process. For a fresh Attempt 5, the resource gate requires zero increase in the cumulative `Swapouts` counter from the immediately pre-launch baseline through process exit, `Pages throttled: 0` throughout, and zero process swaps in `/usr/bin/time -l`; absolute `vm.swapusage used` need not be zero when it is already nonzero before launch. This clarification does not retroactively admit Attempt 4.

EVIDENCE: The preserved Attempt 4 report records exact source `3e5f3095167889b3a9ae5a8145ef0103489e6e3c`, unchanged renderer SHA-256 `e2170a3f1f3a9798bd857f059fdee9b53b0fb2e1d4f16754f244a62236a2d543`, one suite invocation from `2026-09-03T21:44:58Z` to `21:57:58Z`, exit `1` after an intentional `SIGINT`, partial `105` passed tests / `4` passed suites / `0` failure markers, full-suite log SHA-256 `b312dbfef13a38e669408169486294319d1c73a61102d7130b5b0b09939d69f8`, and exit-sidecar SHA-256 `8931b224ed368b91e8713923f47b0bef90d43948778a9e5e7c9e814aeb72d2a1`. The first launch-time resource sample already contained `vm.swapusage used = 4794.25M`, `Swapouts = 892864`, and `Pages throttled = 0`; every preserved active-run supplemental sample kept `Swapouts = 892864` and `Pages throttled = 0`. After interruption, the same cumulative `Swapouts = 892864` and `Pages throttled = 0` remained while allocated swap fell. No render command was issued and `artifacts/day-objects-editorial-field/material/approved-3e5f309-attempt4/` remains absent.

LARGEST GAP: The admitted source still lacks one uninterrupted exact full-suite GREEN at a measurable stable resource baseline; partial passing output cannot prove Gate 0 even though the run produced no observed swapout delta or test failure marker.

NEXT ACCEPTANCE TEST: Commission a fresh isolated producer in new Attempt 5 roots at exact clean commit `3e5f3095167889b3a9ae5a8145ef0103489e6e3c`. Before launch, record frozen-input and renderer hashes plus `Swapouts`, throttled pages, and swap allocation; then invoke `swift test --package-path Tools/DayObjectsEditorialField` exactly once under `/usr/bin/time -l`, with a nonblocking VM-only monitor and persistent log/exit sidecar. Require exit `0`, exact `111/111` tests, `5/5` suites, zero issues, no cumulative `Swapouts` increase, `Pages throttled: 0`, process swaps `0`, unchanged source/renderer, and no material root before completion. Only that result may authorize sequential package rendering; otherwise stop without retry.
