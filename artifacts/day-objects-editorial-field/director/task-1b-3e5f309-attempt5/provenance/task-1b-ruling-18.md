# Task 1B Visual Director Ruling 18 — Baseline Checksum Working Directory

RULING: Preserve the failed `gate6-baseline-checksums` invocation as a path-only producer diagnostic. Authorize exactly one distinct checksum invocation from the immutable baseline package root, using that root's existing `SHA256SUMS` and no writes. No accepted-package or baseline-package render, repair, overwrite, verification, or reseal is authorized. If all checksum entries pass and all pre/post seals remain equal, Gate 6 may be finalized and handed to a fresh independent pre-critic auditor.

EVIDENCE: The failed command declared the baseline worktree root as its working directory while invoking `shasum -a 256 -c artifacts/.../scale1/SHA256SUMS`; the checksum file itself contains package-relative paths such as `actor-crops/...`. Its output consists of `No such file or directory` / `FAILED open or read`, so it evaluated no byte mismatch. It exited `1` after ten seconds with zero process swaps, zero Swapouts delta, and zero throttled pages. The baseline render had already succeeded once at exact commit `622b7b50e0a4594ab47889a2265748e51e94e523`, producing 261 files and package hash `c5fcd68d43b57443c56a7b9934566baf178ed46c5c2ef53d18b90b76b6ba1c51`; the blind packet build independently copied 36/36 source images byte-for-byte, proved 18/18 crop pairs, passed its 37-path allowlist and leak scan, and did not modify accepted packages.

LARGEST GAP: Gate 6 lacks a valid baseline checksum verdict because the only checksum invocation resolved package-relative entries from the wrong directory.

NEXT ACCEPTANCE TEST: From the exact baseline package root, run `shasum -a 256 -c SHA256SUMS` once under a new evidence label and require all declared records PASS, exit0, zero resource breach, and unchanged baseline/accepted package recursive seals before and after. Then finalize the Gate 6 assignment, packet, and checksum ledgers without altering image bytes and commission one fresh isolated read-only pre-critic auditor.
