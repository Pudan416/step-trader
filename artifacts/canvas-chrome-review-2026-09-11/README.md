# Canvas daily chrome review — 2026-09-11

Canvas controls now derive an opaque dark surface, light text and soft accent from the saved daily background palette. This replaces the fixed yellow accent in the Canvas energy pill, data drawer, sound/add controls and shared bottom navigation. Other screens retain their existing color systems.

## Color selection

`TodayCanvasBackdropStore` resolves controls from the same saved pigment source used by Feeds, before snapshot lighting or image sampling. OKLab chroma weights the colored entries; near-gray entries do not wash out the hue. The dominant hue selects a curated sage, blue, lilac or clay family. Dark surfaces adapt by 12% and accents by 18%; an achromatic or balanced input gets neutral tones. Contrast correction preserves readable text and button glyphs. The same input always produces the same output, including reordered palette entries. Palette changes update immediately; restoring the palette through Undo restores the colors. Earning or spending energy does not recolor the UI.

## Layout

Each metric has a label/value line and its own thin progress line. Progress no longer paints under text. Large accessibility text uses stacked titles and values, scaled icon space and the existing scrolling drawer. Controls have opaque tonal backgrounds without glass rims. Existing gestures and the rule that sound/add controls hide while the drawer is open are retained.

## Visual provenance

- `sage.png`, `blue.png`, `lilac.png`, `clay.png`: Simulator captures of the production SwiftUI energy pill, drawer and action buttons, assembled on controlled test gradients. These are component comparisons: the open drawer and bottom actions deliberately appear together for review, unlike the regular app state. Navigation and real Canvas artwork are not included in this fixture.
- `canvas-collapsed.png`, `canvas-expanded.png`: full Canvas screenshots from the targeted UI test, including real artwork, navigation and the existing suggestion card; this simulator has no earned energy or happenings.
- `large.png`: the same component fixture at accessibility3 after adjusting title/value layout and icon space.
- `palette-review.png`: an arrangement of the four unedited screenshots, generated by `make-contact-sheet.swift`.
- `ScreenshotFixture.swift.txt`: temporary debug route used to capture the components; the app route was restored before the production test build and is not changed by this feature.

## Verification

The focused regression run passed 96 tests (0 failures): palette contrast/determinism, energy semantics, Canvas presentation, saved palette/backdrop updates, widget behavior and gate artwork. Contrast checks cover every ModernPaletteCatalog entry plus black, white, gray, empty and opposing saturated inputs. Primary and secondary text and button labels meet 4.5:1; the progress accent against its track meets 3:1.

After the large-text adjustment, all 18 palette/background tests and 3 targeted drawer UI tests passed (21 checks, 0 failures). The UI checks cover row geometry, inline explanation toggles, and hiding/restoring the sound/add controls. Across both runs, 99 distinct checks passed. Simulator captures are not physical-device verification. At the initial review, the last recorded combined installation was `258a9507`; the Play follow-up installation is recorded below.

## Play button follow-up

The sound-start control now shows `play.fill`; playback shows `waveform`, while starting and retry keep their state indicators. The alternate sound-control presentation also uses Play for starting. Actions and accessibility identifiers remain unchanged. `canvas-play.png` is the updated full Canvas screenshot from the UI test. The earlier four-color comparison retains its original speaker icon.

All 34 focused checks passed: 31 presentation checks, the updated rendered-button color check, and two UI checks covering drawer layout and exit from music viewing.

## Combined phone installation

Installed and launched revision `49990fc56e9c927ad6747d660b032cab0221867f` on iPhone Costa on 2026-09-11. A clean Steps4 Debug build from the latest integration head included the new Canvas palette and Play button alongside the widget/settings and gate artwork updates. Strict signature verification passed for the app and all four embedded extensions; all 21 gate images matched between app and ShieldConfiguration. A stale widget App Intents signature in the first incremental build was resolved by a clean build before installation. Launch succeeded; on-device visual and listening evaluation remains manual.
